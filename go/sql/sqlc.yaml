version: "2"
sql:
  - engine: "sqlite"
    queries: "users/queries.sql"
    schema: "users/schema.sql"
    gen:
      go:
        package: "users"
        out: "gen/users"
        overrides:
          - column: "*.id"
            go_type: github.com/google/uuid.UUID
